# Agent 開發鐵律

## 文件同步

- 每次功能、修正或行為變更都必須更新 `docs/CHANGELOG.md`。
- 重大功能、資料流程、部署方式或使用方式變更，必須同步更新 `README.md`。
- 文件要說明使用者可觀察到的行為與重要限制，不要只記錄實作細節。

## 安全

- 絕不提交或輸出 `.env`、API token、密碼、SSH 私鑰、access key 或含憑證的 remote URL。
- 修改遠端環境前先做唯讀盤點，並保留可回滾點。
- Log、錯誤訊息與測試輸出不得包含 secrets。

## 開發與驗證

- 修 bug 或改變行為前，先新增會重現問題的測試。
- 保持每次變更小而可回滾；完成一個切片就測試與提交。
- Supabase 寫入前必須通過嚴格 JSON 驗證。
- `answerbook` 榜單每次執行優先抓最新資料；只有上游抓取失敗時才使用快取。
