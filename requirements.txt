# ==========================================
# Stock Prediction ML Application
# Requirements File
# ==========================================

# Core Dependencies
# ==========================================
python-dotenv>=1.0.0
requests>=2.31.0
urllib3>=2.0.0

# Data Processing & Analysis
# ==========================================
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Stock Market Data
# ==========================================
yfinance>=0.2.0

# Deep Learning Frameworks
# ==========================================
# TensorFlow/Keras (for LSTM and Transformer models)
tensorflow>=2.15.0
keras>=3.0.0
tf-keras  # Compatibility layer for Chronos

# PyTorch (for Cross-sectional models)
torch>=1.13.0

# Time Series Models
# ==========================================
# Prophet (Facebook's time series forecasting)
prophet>=1.1.0

# AutoGluon Chronos (AutoML for time series)
# Note: This package requires statsforecast, which needs C++ compiler (g++) AND python3-dev
# If installation fails, comment this out and set USE_CHRONOS=false in .env
# autogluon.timeseries

# Database
# ==========================================
supabase>=2.0.0

# Optional Dependencies (commented out by default)
# ==========================================
# MongoDB support (only if you need MongoDB)
# pymongo>=4.0.0

# MySQL support (only if you need MySQL)
# mysql-connector-python>=8.0.0

# Cross-sectional models via Qlib (requires C++ compiler)
# Note: Installation may fail on some systems without build tools
# pyqlib

# statsforecast (requires C++ compiler - g++)
# Note: Requires system packages: build-essential, g++
# statsforecast
